sudo: required
services:
  - docker
script:
  - sudo docker build -t hmarks/chucknorris .
deploy:
  provider: script
  script:
    - sudo docker login -u "$REGISTRY_USER" -p "$REGISTRY_PASS"
  provider: script
  script:
    - sudo docker push hmarks/chucknorris
  on:
    branch: dev
