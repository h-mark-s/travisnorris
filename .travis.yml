language: node_js

services:
  - docker

addons:
  ssh_known_hosts:
    - $PUBLICIP

install:
  - echo $KNOWNHOST >> $HOME/.ssh/known_hosts

before_deploy:
  - docker build -t hmarks/chucknorris .

deploy:
  provider: script
  script: bash docker_push
  script: bash deploy
  on:
    branch: dev
