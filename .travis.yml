language: node_js

services:
  - docker

addons:
  ssh_known_hosts:
    - $PUBLICIP

before_deploy:
  - docker build -t hmarks/chucknorris .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: dev

deploy2:
  provider: script
  script: bash deploy
  on:
    branch: dev
